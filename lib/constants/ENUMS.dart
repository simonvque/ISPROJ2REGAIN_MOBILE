enum TextSizes { small, medium, large }

// Constants of HTTP Requests
enum ResponseStatus {
  SAVED,
  FAILED,
  BAD_REQUEST,
  UNAUTHORIZED,
  AUTHORIZED,
  EXPIRED,
  NONE
}

enum TestAddresses { Manila, Makati, Pasay, Paranaque, QuezonC }
